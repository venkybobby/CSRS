-r agent/requirements.txt
-r bff/requirements.txt
# Pinned to the same verified-resolved set as agent/bff's requirements.txt
# (see agent/requirements.txt's header) -- these had never actually been
# installed together with google-adk before and needed re-verifying too.
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-mock==3.15.1
ruff==0.16.2
mypy==2.3.0
pyyaml==6.0.3
psycopg2-binary==2.9.12
