# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/

# Node / frontend
node_modules/
dist/
.vite/

# Env / secrets
.env
.env.*
!.env.example
*.key.json
*-credentials.json
*service-account*.json

# Cloud SQL local proxy binaries
cloud-sql-proxy
cloud-sql-proxy.exe

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code plugin cache (runtime-generated, not project data)
.claude/data/
*.tsbuildinfo

# Claude Code runtime state for sessions run from infra/envs/ -- transcripts
# can carry anything typed or pasted during a debugging session in that
# directory (real endpoint values, output from gcloud/terraform commands
# against real infra), so this is a repo-sweep finding, not a style choice:
# unlike .claude/ at the repo root (project config, meant to be tracked),
# this one is pure runtime log and was sitting untracked and unprotected.
infra/envs/.claude/

# Recorded demo takes. Regenerable by scripts/record_demo_video.py and large
# (~5 MB each), and a video is re-recorded per take rather than maintained --
# unlike docs/screenshots/, which is committed precisely because it is the
# record of what a CSR sees and must be diffable.
docs/demo/*.webm
docs/demo/*.mp4
